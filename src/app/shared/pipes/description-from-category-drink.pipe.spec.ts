import { DescriptionFromCategoryDrinkPipe } from './description-from-category-drink.pipe';

describe('DescriptionFromCategoryDrinkPipe', () => {
  it('create an instance', () => {
    const pipe = new DescriptionFromCategoryDrinkPipe();
    expect(pipe).toBeTruthy();
  });
});
